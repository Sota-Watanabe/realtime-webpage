config:
  target: "ws://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 1
scenarios:
  - engine: "ws"
    flow:
      # - send: "hello"
      - think: 20 # pause for 1 second
      # - loop:
      #     - send: "world"
      #   count: 5